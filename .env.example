# General app config
TZ=UTC
PORT=3333
HOST=0.0.0.0
LOG_LEVEL=info
APP_KEY=
NODE_ENV=production
SESSION_DRIVER=cookie

# PostgreSQL (use DATABASE_URL for deployment, individual vars for dev)
DB_CONNECTION=pg
DATABASE_URL=

# Optional: only for local development
DB_HOST=127.0.0.1
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_DATABASE=

# SMTP (e.g., Gmail SMTP)
SMTP_HOST=
SMTP_PORT=
